package hbone

import "testing"

// WIP
func TestHBone(t *testing.T) {
	// Setup alice and bob, from testdata. The certs are long-lived.

	// Start echo server for bob, client for alice, using hbonecat mtls and plain text
}
